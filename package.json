{
  "name": "pi-memory-compact",
  "version": "0.1.0",
  "description": "pi-agent (pi CLI) extension: memory-anchored compaction — keep the opening N messages verbatim, retrieve related memories from pi's own session history/tree, fix the system prompt, and fold everything else into one structured summary.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test \"test/*.test.ts\"",
    "typecheck": "tsc -p tsconfig.json --noUnusedLocals --noUnusedParameters"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22.19"
  }
}
